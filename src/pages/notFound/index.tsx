import React, { FC } from 'react';
import { NotFoundPageContent } from 'src/components/sections/NotFoundPageContent';

const NotFound: FC = () => {
  return <NotFoundPageContent />;
};

export default NotFound;
